<script type="text/javascript">
  var slideshow;
  var code_editor;
  $(function() {
    code_editor = CodeMirror(document.body, {
      mode:  "javascript"
    })

    slideshow = create_slideshow(<%= @show.to_json(:include => :slides).html_safe %>);
  })
</script>

<%= render :partial => 'slides/slide', :collection => @show.slides %>
