<div id="slide_<%=slide.id%>" class='slide zoomed_in_slide slide_transition'>
  <svg></svg>
  <%= form_for(slide, :remote => true) do |f|%>
    <%= f.hidden_field :scripts %>
    <%= f.submit %>
  <% end %>
</div>

